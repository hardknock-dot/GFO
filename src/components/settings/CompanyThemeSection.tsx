import React from 'react';

interface CompanyThemeSectionProps {
  companyId?: string;
  canModify: boolean;
}

export const CompanyThemeSection: React.FC<CompanyThemeSectionProps> = () => {
  return null;
};
